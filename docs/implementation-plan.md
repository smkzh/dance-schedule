# 実装計画（勉強計画）

作成日時: 2026年4月2日

各ステップは「動くものを作って確認する」を繰り返す。
ステップをまたいで進まず、1つ完成させてから次へ。

---

## Phase 1: 環境構築

### Step 1: Node.js と Next.js プロジェクトの作成
**学ぶこと**
- Node.js / npm とは何か
- Next.js プロジェクトの構成（どのファイルが何をするか）
- TypeScript, Tailwind CSS の初期設定

**やること**
```
Node.js をインストール
→ npx create-next-app でプロジェクト作成
→ VS Code で開く
→ npm run dev でブラウザに "Hello World" を表示
```

**完了の確認**
- `http://localhost:3000` をブラウザで開いてページが表示される

---

### Step 2: ページとルーティングを理解する
**学ぶこと**
- App Router の仕組み（フォルダ = URL）
- `page.tsx` とは何か
- リンクの作り方（`<Link>`）

**やること**
```
app/
  page.tsx         → /
  submit/
    page.tsx       → /submit
  numbers/
    page.tsx       → /numbers
  admin/
    upload/
      page.tsx     → /admin/upload
```
各ページに仮のテキストを置いてリンクでつなぐ

**完了の確認**
- ホームから各ページへリンクで移動できる

---

### Step 3: コンポーネントと Tailwind CSS を理解する
**学ぶこと**
- React コンポーネントとは何か（UI の部品）
- props の渡し方
- Tailwind CSS の書き方（クラスでスタイルを当てる）
- レスポンシブ対応（`sm:` `md:` プレフィックス）

**やること**
- ホームページにボタンを2つ置いてスタイルを当てる
- スマホ・PCで見た目が変わることを確認する

**完了の確認**
- ブラウザの幅を変えるとレイアウトが変わる

---

## Phase 2: データベースの準備

### Step 4: Supabase プロジェクトの作成とテーブル設計
**学ぶこと**
- Supabase とは何か
- SQL の基本（CREATE TABLE, INSERT, SELECT）
- リレーション（外部キー）とは何か

**やること**
```sql
-- Supabase のダッシュボードで以下のテーブルを作成
performances  （公演）
members       （メンバー）
numbers       （ナンバー）
number_members（出演者の紐付け）
availabilities（空き日程）
```
テーブル作成後、ダッシュボードから手動でテストデータを入れる

**完了の確認**
- Supabase のテーブルエディタでデータが表示される

---

### Step 5: Next.js から Supabase に接続してデータを取得する
**学ぶこと**
- 環境変数（`.env.local`）とは何か
- Supabase クライアントの作り方
- サーバーコンポーネントでデータを取得する方法

**やること**
- Supabase のライブラリをインストール
- Step 4 で入れたテストデータをホームページに表示する

**完了の確認**
- ブラウザに Supabase から取得した公演名が表示される

---

## Phase 3: 管理機能（CSV アップロード）

### Step 6: CSV ファイルをアップロードしてパースする
**学ぶこと**
- フォームと `<input type="file">` の使い方
- CSV を JavaScript でパースする方法
- API ルート（`route.ts`）とは何か

**やること**
- `/admin/upload` にファイルアップロードフォームを作る
- CSV を読み込んで内容をブラウザに表示する（まだ保存しない）

**完了の確認**
- CSV をアップロードすると、中身がブラウザに一覧表示される

---

### Step 7: CSV のデータを Supabase に保存する
**学ぶこと**
- Supabase への INSERT の書き方
- トランザクション（複数テーブルへの一括保存）の考え方
- エラーハンドリングの基本

**やること**
- Step 6 でパースしたデータを Supabase の各テーブルに保存する
- 既存の公演を非アクティブにしてから新しい公演を登録する（`is_active` の切り替え）

**完了の確認**
- CSV をアップロードすると Supabase のテーブルにデータが入る

---

## Phase 4: 日程提出機能

### Step 8: 名前選択と状態管理を理解する
**学ぶこと**
- `useState` とは何か（コンポーネント内の状態）
- セレクトボックスの作り方
- クライアントコンポーネント（`"use client"`）とは何か

**やること**
- `/submit` にメンバー名のドロップダウンを作る
- 名前を選ぶと選択中の名前が表示される

**完了の確認**
- ドロップダウンで名前を選ぶと画面に反映される

---

### Step 9: カレンダーコンポーネントを作る
**学ぶこと**
- 日付の操作（JavaScript の `Date` オブジェクト）
- 繰り返し処理（`map`）でカレンダーのマス目を作る
- クリックイベントの処理

**やること**
- 月のカレンダーを表示する
- 日付をタップすると選択状態になる（色が変わる）
- 前月・次月に移動できる

**完了の確認**
- カレンダーが表示され、日付をタップすると色が変わる

---

### Step 10: 時間スロットグリッドを作る
**学ぶこと**
- 配列から UI を生成する方法
- 複数の要素をまとめて選択する操作（ドラッグ選択）

**やること**
- カレンダーで日付を選ぶと、09:00〜21:30 の 30 分スロットが縦に並んで展開される
- タップで選択・解除できる
- ドラッグで連続したスロットをまとめて選択できる

**完了の確認**
- 日付を選ぶとスロットが展開され、タップで選択できる

---

### Step 11: 日程データを Supabase に保存・再提出できるようにする
**学ぶこと**
- Supabase への UPSERT（上書き保存）の書き方
- 既存データの取得と画面への反映

**やること**
- 「提出する」ボタンを押すと `availabilities` テーブルに保存される
- 再度アクセスすると前回の提出内容が復元される
- 修正して再提出すると上書きされる

**完了の確認**
- 提出 → ページを再読み込み → 前回の選択が残っている

---

## Phase 5: 候補日確認機能

### Step 12: ナンバー一覧ページを作る
**学ぶこと**
- 動的ルーティング（`[id]`）の仕組み
- Supabase のテーブル結合（JOIN）の書き方

**やること**
- `/numbers` にナンバーの一覧を表示する
- 各ナンバーから `/numbers/[id]` へ遷移できる

**完了の確認**
- ナンバーの一覧が表示され、タップで詳細ページに移動できる

---

### Step 13: 候補日の計算と表示
**学ぶこと**
- 複雑なデータ集計の考え方
- データを加工して UI に表示する方法

**やること**
- ナンバーの出演者の `availabilities` を取得する
- 振り付け者が空いている時間帯に絞る
- 連続 3 スロット（90 分）が揃う「日付 + 開始時刻」を列挙する
- 出席人数が多い順に並べて表示する

**完了の確認**
- 候補日が出席人数の多い順に表示される

---

## Phase 6: 仕上げ

### Step 14: ホームページを整える
**やること**
- 公演名を表示する
- 「日程を提出する」「候補日を確認する」ボタンを配置する
- 全体のデザインを整える

---

### Step 15: Vercel にデプロイして URL を発行する
**学ぶこと**
- GitHub へのプッシュの仕方
- Vercel の設定（環境変数の設定）

**やること**
- GitHub にコードをプッシュする
- Vercel と連携して自動デプロイを設定する
- 本番 URL でスマホから動作確認する

**完了の確認**
- スマホのブラウザで URL を開いて操作できる

---

## まとめ

| Phase | Step | 内容 |
|---|---|---|
| 1. 環境構築 | 1 | Node.js・Next.js プロジェクト作成 |
| | 2 | ページとルーティング |
| | 3 | コンポーネントと Tailwind CSS |
| 2. DB 準備 | 4 | Supabase テーブル作成 |
| | 5 | Next.js から Supabase 接続 |
| 3. 管理機能 | 6 | CSV アップロード・パース |
| | 7 | CSV データを Supabase に保存 |
| 4. 日程提出 | 8 | 名前選択・状態管理 |
| | 9 | カレンダーコンポーネント |
| | 10 | 時間スロットグリッド |
| | 11 | 提出・再提出の保存 |
| 5. 候補日確認 | 12 | ナンバー一覧ページ |
| | 13 | 候補日の計算と表示 |
| 6. 仕上げ | 14 | ホームページ整備 |
| | 15 | Vercel デプロイ |
